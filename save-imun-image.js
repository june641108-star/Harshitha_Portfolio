const fs = require('fs');
const https = require('https');
const http = require('http');

// The image needs to be saved manually. 
// For now, let's check if user has already placed it or we need to copy from conversation.
console.log("Image save script ready. The IMUN conference image needs to be placed at: public/imun-conference.jpg");
